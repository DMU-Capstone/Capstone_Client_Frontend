import React from "react";
import {
    View, Text, StyleSheet, TextInput, TouchableOpacity
} from 'react-native';

export const Card: React.FC = () => {
    return(
        <View>
            <Text></Text>
        </View>
    );
}